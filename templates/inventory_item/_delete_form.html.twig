<form method="post" action="{{ path('app_inventory_item_delete', {'inventoryItemId': inventory_item.id, 'inventoryId': inventory_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inventory_item.id) }}">
    <button class="btn">Delete</button>
</form>
