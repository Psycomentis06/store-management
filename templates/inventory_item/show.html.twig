{% extends 'base.html.twig' %}

{% block title %}InventoryItem{% endblock %}

{% block body %}
    <h1>InventoryItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inventory_item.id }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ inventory_item.stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inventory_item_index') }}">back to list</a>

    <a href="{{ path('app_inventory_item_edit', {'id': inventory_item.id}) }}">edit</a>

    {{ include('inventory_item/_delete_form.html.twig') }}
{% endblock %}
